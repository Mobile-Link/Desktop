<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vmNoAuth="clr-namespace:MobileLink_Desktop.ViewModels.NoAuth"
             x:Class="MobileLink_Desktop.Views.NoAuth.Login"
             x:DataType="vmNoAuth:LoginViewModel"
             >
    <StackPanel>
        <!-- TODO Not centering -->
        <TextBlock Text="Faça seu login" />
        <TextBox Text="{Binding EmailUser}"></TextBox>
        <TextBox Text="{Binding Password}"></TextBox>
        <Button Command="{Binding SubmitLogin}">Entrar</Button>
    </StackPanel>
</UserControl>
